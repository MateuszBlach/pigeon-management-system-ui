const baseUrl = 'http:/13.60.89.58:8080'

export const backend = {
  baseUrl: baseUrl,
  userUrl: baseUrl + '/user',
  pigeonUrl: baseUrl + '/pigeon',
  flightUrl: baseUrl + '/flight',
  flightRecordUrl: baseUrl + '/flight-record',
}
